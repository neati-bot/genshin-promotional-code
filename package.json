{
  "name": "genshin-promotional-code",
  "version": "0.3.0",
  "private": true,
  "description": "Genshin Impact Asia-server promotional code notifier (Discord webhook via GitHub Actions cron).",
  "type": "module",
  "scripts": {
    "check": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
